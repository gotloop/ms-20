import { describe, it, expect, beforeEach } from "vitest";
import "./web-audio.stub";
import { ScaleValue, state } from "./state";
import "./effects";

describe("effects", () => {
	describe("frequency", () => {
		it("should set value of webaudio oscillator", () => {});
	});
});
